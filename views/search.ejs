<%- include('partials/header') %>

<div class="container mx-auto px-4 py-8 min-h-screen">
    
    <div class="max-w-3xl mx-auto text-center mb-12">
        <h1 class="text-3xl font-bold text-white mb-4">
            Hasil Pencarian
        </h1>
        
        <form action="/search" method="GET" class="relative">
            <input type="text" name="q" value="<%= query %>" 
                class="w-full text-white border border-gray-700 rounded-full py-4 pl-6 pr-14 focus:outline-none focus:border-pink-500 focus:ring-1 focus:ring-pink-500 transition text-lg shadow-lg placeholder-gray-500" 
                placeholder="Ketik judul anime, manga, atau berita..." autofocus>
            
            <button type="submit" class="absolute right-3 top-2.5 bg-pink-600 hover:bg-pink-700 text-white p-2 rounded-full transition shadow-md">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
            </button>
        </form>

        <% if (query) { %>
            <p class="text-gray-400 mt-4">
                Menampilkan hasil untuk kata kunci: <span class="text-pink-500 font-bold">"<%= query %>"</span>
            </p>
        <% } %>
    </div>

    <% if(posts.length > 0) { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <% posts.forEach(post => { %>
            <div class="rounded-xl overflow-hidden shadow-md border border-gray-700 hover:border-pink-500 hover:-translate-y-1 transition duration-300 group flex flex-col h-full">
                <div class="h-48 overflow-hidden relative">
                    <img src="<%= post.image.startsWith('http') ? post.image : '/uploads/' + post.image %>" 
                         class="w-full h-full object-cover transform group-hover:scale-110 transition duration-500">
                    <div class="absolute top-2 left-2">
                        <span class="bg-gray-900/80 backdrop-blur text-white text-[10px] font-bold px-2 py-1 rounded border border-gray-600">
                            <%= post.category[0] %>
                        </span>
                    </div>
                </div>

                <div class="p-4 flex flex-col flex-grow">
                    <h3 class="text-white font-bold text-base mb-2 line-clamp-2 group-hover:text-pink-400 transition leading-snug">
                        <a href="/read/<%= post.slug %>"><%= post.title %></a>
                    </h3>
                    
                    <p class="text-gray-400 text-sm line-clamp-2 mb-4">
                        <%- post.content.replace(/(<([^>]+)>)/ig,"").substring(0, 80) %>...
                    </p>

                    <div class="mt-auto pt-3 border-t border-gray-700 flex justify-between items-center text-xs text-gray-500">
                        <span class="flex items-center gap-1">
                            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
                            <%= post.createdAt.toLocaleDateString('id-ID') %>
                        </span>
                        <a href="/read/<%= post.slug %>" class="text-pink-400 hover:text-white transition font-semibold">Baca &rarr;</a>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
    <% } else { %>
        <div class="flex flex-col items-center justify-center py-16 text-center">
            <div class="p-6 rounded-full mb-4">
                <svg class="w-16 h-16 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            </div>
            <h3 class="text-2xl font-bold text-white mb-2">Tidak Ditemukan</h3>
            <p class="text-gray-400 max-w-md">
                Maaf, kami tidak dapat menemukan berita dengan kata kunci <span class="text-pink-500 font-bold">"<%= query %>"</span>. Coba gunakan kata kunci lain.
            </p>
            <div class="mt-8">
                <a href="/" class="bg-gray-700 hover:bg-gray-600 text-white px-6 py-2 rounded-lg font-bold transition">Kembali ke Beranda</a>
            </div>
        </div>
    <% } %>
</div>

<%- include('partials/footer') %>